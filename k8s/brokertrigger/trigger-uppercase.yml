apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: trigger-uppercase
spec:
  broker: demo-broker
  filter:
    attributes:
      type: uppercase
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: func-uppercase

